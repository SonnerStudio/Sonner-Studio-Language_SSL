[package]
name = "ssl-v5"
version = "5.0.0-dev"
description = "Sonner Studio Language v5.0 - Self-Hosted Compiler"
authors = ["Sonner Studio"]
license = "MIT"

[dependencies]
# No external dependencies - fully self-contained

[build]
entry = "src/main.ssl"
output = "ssl"
