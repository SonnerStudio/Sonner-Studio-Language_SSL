fn main() {
    print("Complex Args Test")
    let args = env_args()
    let count = len(args)
    if count > 0 {
        let last = args[count - 1]
        print("Last arg: " + last)
    }
    return 0
}
main()
