## 🌍 言語 / Languages

**[English](DOCUMENTATION.md)** | **[Deutsch](DOCUMENTATION_DE.md)** | **[Français](DOCUMENTATION_FR.md)** | **[Español](DOCUMENTATION_ES.md)** | **[Português](DOCUMENTATION_PT.md)** | **[日本語](DOCUMENTATION_JA.md)**

---

# SSL ドキュメント

## 目次

1. [紹介](#紹介)
2. [機能](#機能)
3. [インストール](#インストール)
4. [はじめに](#はじめに)
5. [構文](#構文)
6. [組み込み関数](#組み込み関数)
7. [例](#例)
8. [FAQ](#faq)

---

## 紹介

Sonner Studio Language (SSL)は、コンピューティングの未来のために設計された実験的なAIネイティブプログラミング言語です。以下を組み合わせています：

- **並列処理優先設計**: スレッドとチャネルによるネイティブ並行性
- **量子コンピューティング**: 統合量子シミュレータ
- **自己修復コード**: AI支援による自動エラー回復
- **モダンな型システム**: 推論機能付きハイブリッド静的/動的型付け

---

## 機能

### 1. 変数と型

```ssl
let x = 10
mut count = 0
count = count + 1
let name: String = "SSL"
```

### 2. 関数

```ssl
fn add(a: Int, b: Int) -> Int {
    return a + b
}
```

### 3. 制御フロー

```ssl
if x > 10 {
    print("大きい")
} else {
    print("小さい")
}

for i in 0..10 {
    print(i)
}
```

### 4. 並列プログラミング

```ssl
let chan = channel()

spawn {
    send(chan[0], 42)
}

let result = recv(chan[1])
print(result)
```

### 5. 量子コンピューティング

```ssl
let q = Qubit()
H(q)  // Hadamard: 重ね合わせ
let result = Measure(q)
print(result)  // 0または1 (50/50)
```

### 6. エラー処理

```ssl
try {
    let result = 危険な操作()
} recover (err) {
    print("エラー:", err)
}
```

---

## インストール

```bash
git clone https://github.com/SonnerStudio/Sonner-Studio-Language_SSL.git
cd Sonner-Studio-Language_SSL
cargo build --release
```

---

## はじめに

`hello.ssl`を作成:

```ssl
fn main() {
    print("こんにちは、世界!")
}
```

実行:

```bash
ssl run hello.ssl
```

---

## 構文

### データ型

- `Int`: 整数 (64-bit)
- `String`: 文字列
- `Bool`: ブール値 (true/false)
- `Qubit`: 量子ビット

### 演算子

**算術**: `+`, `-`, `*`, `/`
**比較**: `==`, `!=`, `<`, `>`, `<=`, `>=`

---

## 組み込み関数

- `print(...args)`: コンソールに表示
- `channel()`: 通信チャネルを作成
- `send(sender, value)`: 値を送信
- `recv(receiver)`: 値を受信
- `spawn { ... }`: スレッドを開始
- `Qubit()`: 量子ビットを作成
- `H(qubit)`: Hadamardゲート
- `Measure(qubit)`: 量子ビットを測定

---

## 例

### プロデューサー・コンシューマーパターン

```ssl
fn main() {
    let chan = channel()
    
    spawn {
        for i in 0..10 {
            send(chan[0], i)
        }
    }
    
    for i in 0..10 {
        print("受信:", recv(chan[1]))
    }
}
```

---

## FAQ

**SSLは本番環境対応ですか？**
いいえ、SSLは実験的です。

**どのプラットフォームがサポートされていますか？**
すべてのRustプラットフォーム（Windows、macOS、Linux）。

---

**❤️とRustで構築** 🦀
