// Demo: AI Code Review
// Run with: ssl run examples/ai_review_demo.ssl --ai-review
// Note: Requires OPENAI_API_KEY environment variable

fn calculateDiscount(price: Int, discountPercent: Int) -> Int {
    // Potential issue: No validation of inputs
    let discount = price * discountPercent / 100
    return price - discount
}

fn unsafeOperation(data: String) -> String {
    // Security issue: No input sanitization
    return data
}

let finalPrice = calculateDiscount(100, 20)
print("Final price: ")
print(finalPrice)
